[user]
	name = Vic
	email = 95343562+xxheyhey@users.noreply.github.com
[core]
	editor = nvim
[alias]
    # git change-commits GIT_COMMITTER_NAME/GIT_AUTHOR_NAME/GIT_AUTHOR_EMAIL "old name" "new name"
change-commits = "!f() { VAR=$1; OLD=$2; NEW=$3; shift 3; git filter-branch --env-filter \"if [[ \\\"$`echo $VAR`\\\" = '$OLD' ]]; then export $VAR='$NEW'; fi\" $@; }; f "
